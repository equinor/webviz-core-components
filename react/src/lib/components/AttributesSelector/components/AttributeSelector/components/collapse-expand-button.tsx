import React from "react";

export type CollapseExpandButtonProps = {
    expanded: boolean;
};

export const CollapseExpandButton: React.FC<CollapseExpandButtonProps> = (
    props
) => {
    return <div>Keep options open</div>;
};
